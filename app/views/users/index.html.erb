<h1>Users</h1>
<table>
  <tr>
  <th>name</th>
  <th>email</th>
  <th>Actions</th>
  </tr>
  <% @users.map do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
  <% end %>
<table>
<%= link_to 'New User', new_user_path %>